# Snake Xenzia — Red/Black Stable v5.0

- Solid engine (proper loop, speed ramp, obstacles, fruit, SFX).
- **Code gate** with password-style input (asterisks). No hint on failure — just “Code failed”.
- Accepts `slvlog2.0` (case-insensitive; ignores spaces/punctuation).
- Two-step intro messages; wait-to-start.
- **Win at 2000** → BIG bold message: “CONGRATS SCAVENGER ... AAX 4399”.

Unzip, keep `assets/` next to `index.html`, open `index.html`.
