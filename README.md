# Snake Xenzia — Wait-to-Start (Fix)

- Fixed a JavaScript bug (`True` -> `true`) that broke the **Next** button.
- Still: snake waits for first key press to move, mic sprite fruit, 3000-point win, victory code shown.

Unzip, keep `assets/` with `index.html`, and open `index.html`.
